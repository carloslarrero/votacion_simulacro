import 'package:votacion_simulacro/barril.dart';

class AppBarUtils extends StatelessWidget {
  const AppBarUtils({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
